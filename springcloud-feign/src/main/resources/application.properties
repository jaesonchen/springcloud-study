spring.application.name = feign-consumer

server.port = 9020

# 注册和发现服务Eureka Server地址，多个eureka以逗号分隔。
eureka.client.serviceUrl.defaultZone = http://localhost:8000/eureka/

## D版本之后feign中的hystric默认关闭
feign.hystrix.enabled = true
